services:
  backend:
    image: photos-backend
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-production
    command: poetry run gunicorn photos.wsgi -c ./app/build/gunicorn.conf.py
    ports:
      - 1337:1337
